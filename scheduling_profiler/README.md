# Scheduling Profiler
## How to Use
1. Start profiling.
    ```
    sh start_profile.sh
    ```
2. Stop profiling. Result will be created to the current directory with `ftrace.log.c`
    ```
    get_log.sh
    ```